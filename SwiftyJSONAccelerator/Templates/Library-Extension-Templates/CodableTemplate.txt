{DISABLE_SWIFT_LINT_FORCECAST}
/// Designated Initializer: Initialize the instance based on the parameters
    {INIT_OVERRIDE}init({INIT_PARAMETERS}) {
        {INITIALIZER}
        {SUPER_INIT_CALL}
    }

    // MARK: Codable Protocol
/// CodingKeys: alternative keys for the properties
    {CODABLE_CODING_KEYS_ENUM}

/// decode the decoder and create self instance
///
/// - Parameter decoder: decoder instance to be decoded
/// - Throws: `DecodingError.typeMismatch` if the encountered encoded value
///   is not convertible to the requested type.
/// - Throws: `DecodingError.keyNotFound` if `self` does not have an entry
///   for the given key.
/// - Throws: `DecodingError.valueNotFound` if `self` has a null entry for
///   the given key.
    required public init(from decoder: Decoder) throws {
        {CODABLE_DECODERS_VALUE}
        {CODABLE_DECODERS}
        {CODABLE_DECODERS_SUPER_INIT_CALL}
    }

/// encode the encoder value
///
/// - Parameter encoder: encoder
/// - Throws: `EncodingError.invalidValue` if the given value is invalid in
///   the current context for this format.
    public{CODABLE_ENCODE_OVERRIDE}func encode(to encoder: Encoder) throws {
        {CODABLE_ENCODERS_SUPER_INIT_CALL}
        {CODABLE_ENCODERS_CONTAINER}
        {CODABLE_ENCODERS}
    }
